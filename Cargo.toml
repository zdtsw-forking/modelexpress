# SPDX-FileCopyrightText: Copyright (c) 2025-2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
# SPDX-License-Identifier: Apache-2.0

[workspace]
members = [
    # Directory names remain for path, but crates are published as:
    "modelexpress_server", # modelexpress-server
    "modelexpress_client", # modelexpress-client
    "modelexpress_common", # modelexpress-common
    "operator",
    "xtask",
    "workspace-tests",
]
resolver = "3"

[workspace.package]
version = "0.5.1"
edition = "2024"
description = "Model Express: High-performance model serving and management"
authors = ["NVIDIA Inc. <sw-dl-dynamo@nvidia.com>"]
license = "Apache-2.0"
homepage = "https://github.com/ai-dynamo/modelexpress"
repository = "https://github.com/ai-dynamo/modelexpress.git"
keywords = ["llm", "inference", "nvidia"]

[workspace.dependencies]
anyhow = { version = "1.0.98", features = ["backtrace"] }
async-trait = "0.1"
# Default features off deliberately. axum already reaches the lock as a
# `default-features = false` transitive dependency of tonic; turning defaults on
# for a GET-only /metrics route would add form/json/query extractors and pull
# serde_path_to_error into the server binary for nothing.
axum = { version = "0.8", default-features = false, features = ["http1", "tokio"] }
chrono = { version = "0.4", features = ["serde"] }
clap = { version = "4.5", features = ["derive", "env"] }
config = { version = "0.15", features = ["yaml", "toml", "json"] }
crc32c = "0.6.8"
colored = "3.0.0"
fd-lock = "4.0.4"
google-cloud-gax = "1.8.0"
google-cloud-storage = { version = "1.9.0", default-features = false }
hf-hub = { version = "0.4.3", default-features = false, features = ["tokio"] }
jiff = { version = "0.2.15", features = ["serde"] }
modelexpress-common = { path = "modelexpress_common", version = "0.5.1", default-features = false }
modelexpress-client = { path = "modelexpress_client", version = "0.5.1", default-features = false }
modelexpress-server = { path = "modelexpress_server", version = "0.5.1", default-features = false }
once_cell = "1.21.3"
object_store = { version = "0.14.1", default-features = false, features = ["aws-base", "reqwest"] }
object-store-reqwest = { package = "reqwest", version = "0.13", default-features = false }
# Prometheus instrumentation (client_rust). Registration and `get_or_create` are
# infallible, which is required here: the workspace denies `unwrap_used` and
# `expect_used`, ruling out the `register_*!(...).unwrap()` idiom of the tikv crate.
prometheus-client = { version = "0.25", default-features = false }
prost = "0.13"
rustls = { version = "0.23.37", default-features = false }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
mockall = "0.14.0"
tempfile = "3.20"
tokio = { version = "1.46", features = ["full"] }
tokio-stream = "0.1"
tonic = "0.13"
tonic-build = "0.13"
tonic-health = "0.13"
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
futures = "0.3"
uuid = { version = "1.17", features = ["v4", "serde"] }
thiserror = "2.0"
tower = { version = "0.5.3", features = ["util"] }
http = "1.4.1"
moka = { version = "0.12.15", features = ["future"] }
secrecy = "0.10.3"
redis = { version = "0.27", features = ["tokio-comp", "connection-manager"] }
reqwest = { version = "0.12", default-features = false, features = ["json", "stream"] }

# Kubernetes client (kube-rs) for CRD-based metadata backend
kube = { version = "0.98", features = [
    "client",
    "runtime",
    "derive",
], default-features = false }
k8s-openapi = { version = "0.24", features = ["v1_31"] }
schemars = { version = "0.8", features = ["chrono"] }
sha2 = "0.10"
base64 = "0.22"
serde_norway = "0.9"

[workspace.lints.clippy]
all = { level = "deny", priority = -1 }
#pedantic = { level = "deny", priority = -1 }
#cargo = { level = "deny", priority = -1 }
style = { level = "deny", priority = -1 }
perf = { level = "deny", priority = -1 }
suspicious = { level = "deny", priority = -1 }
complexity = { level = "deny", priority = -1 }
mod_module_files = "deny"
self_named_module_files = "allow"
alloc_instead_of_core = "deny"
arithmetic_side_effects = "deny"
pub_use = "allow"
pub_without_shorthand = "deny"
pub_with_shorthand = "allow"
arbitrary_source_item_ordering = "allow"
implicit_return = "allow"
single_call_fn = "allow"
question_mark_used = "allow"
print_stdout = "allow"
use_debug = "allow"
todo = "allow"
shadow_same = "allow"
shadow_reuse = "allow"
shadow_unrelated = "allow"
unwrap_used = "deny"
expect_used = "deny"
allow_attributes = "allow"
missing_errors_doc = "allow"


[workspace.lints.rust]
#missing_docs = "warn"
